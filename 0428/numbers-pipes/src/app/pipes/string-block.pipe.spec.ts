import { StringBlockPipe } from './string-block.pipe';

describe('StringBlockPipe', () => {
  it('create an instance', () => {
    const pipe = new StringBlockPipe();
    expect(pipe).toBeTruthy();
  });
});
